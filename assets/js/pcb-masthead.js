/* assets/js/pcb-masthead.js */

(function (window) {
  "use strict";

  if (window.MMJ && window.MMJ.header && typeof window.MMJ.header.initAll === "function") {
    window.MMJ.header.initAll();
  }
})(window);
